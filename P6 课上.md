## 										<center>P6 课上</center>

<center><small>Created 2025-11-18 | 1:25 || 蘑菇干</small></center>

---

### T1 MyGo

描述：根据公式计算 GPR[rs] 和 GPR[rt] 的复杂度，将复杂度高的存入 HI，低的存入 LO。

```tex
def:
	pre_0()：对于 4 位二进制，前导 0 的个数
	cnt_1()：对于 4 位二进制，所含 1 的个数
	def1()： 对于 4 位二进制，2*pre_0 + cnt_1
	complication()：将 32 位数 x 分割 8 个 4 位二进制，从低到高依次为 g0~g7
					complication(x) = sigma[i = 0~7]def1(gi)*(i + 1)
I:
	comp_rs		<--  complication(GPR[rs]))
	comp_rt		<--  complication(GPR[rt]))
	if comp_rs > comp_rt:
		HI      <--  GPR[rs]
		LO      <--  GPR[rt]
	if comp_rs < comp_rt:
		HI      <--  GPR[rt]
		LO      <--  GPR[rs]
	if comp_rs = comp_rt:
		HI      <--  GPR[rs] ^ GPR[rt]
		LO      <--  GPR[rs] ^ GPR[rt]
	endif
```

```tex
有关延迟：complication()的计算非常复杂，所以和 mult 一样需要延时计算 5 个周期。
```



### T2 Jcfx

先求 GPR[rs] 和 GPR[rt] 异或的低五位，找到小于等于它的最大完全平方数 x，无条件跳转，然后链接到 GPR[x]。

```tex
def:
	is_perfect()：  是否为完全平方数
I:
	temp	<--		GPR[rs] ^ GPR[rt]
	x		<--		temp[4:0]
	while (~is_perfect(x)) 
		x = x - 1
	GPR[x]  <--     pc + 8
	branch like beq in any condition
```

```tex
完全平方数 x，指存在 k，使得 x = k*k
```





### T3 Lbo

和 `lb` 一样读出一个字节存到 `temp`，遍历 `GPR[rt]` 的每一个字节 `g_i`（i = 0~3） ，若 `temp != g_i`，且 `temp` 中 1 的个数 > `g_i` 中 1 的个数，则用 `temp` 取代该字节，然后把结果像 `lw` 一样存入。





